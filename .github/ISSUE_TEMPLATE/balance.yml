name: Daily balance
description: Enter today's actual bank balance. This is the only manual step.
title: "Balance"
labels: ["balance"]
body:
  - type: input
    id: amount
    attributes:
      label: Today's actual balance
      description: What the bank says right now, including pending. Just the number.
      placeholder: "4382.17"
    validations:
      required: true
